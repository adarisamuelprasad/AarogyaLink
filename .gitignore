# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# Explicitly ignore node_modules in client and server subdirectories
/client/node_modules
/server/node_modules

# testing
/coverage

# production build artifacts
/build
/client/build # Explicitly ignore client build folder if it's not directly under root /build
/server/dist  # If your server builds any distribution files

# misc
.DS_Store

# Environment variables
.env               # General .env file (e.g., for server secrets)
*.env              # Catch all .env files (e.g., .env.production, .env.development)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE and OS specific files
.vscode/           # Visual Studio Code settings